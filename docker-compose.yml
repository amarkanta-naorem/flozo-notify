services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: flozo-notify-staging

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "127.0.0.1:5005:5005"